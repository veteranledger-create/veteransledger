/**
 * CONFIG MODULE
 * Re-exports site configuration for ES module imports.
 *
 * Usage:
 *   import { SITE, NAVIGATION, DATA_SOURCES } from '@core/config';
 */

export { SITE, NAVIGATION, LEGAL_LINKS, THEME, DATA_SOURCES } from '../../config/site.js';
